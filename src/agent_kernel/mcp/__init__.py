"""Hand-rolled MCP (DESIGN.md §6). Written from scratch on purpose — the official
SDK is reference only. The client (M2) connects out to one real server; the
server (M5) exposes the kernel's own tools. Both are stubs until then.
"""
